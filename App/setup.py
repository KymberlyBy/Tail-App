from setuptools import setup, find_packages

setup(name = 'models', packages = find_packages())
setup(name = 'controllers', packages = find_packages())
setup(name = 'views', packages = find_packages())